/**
 * Shared type definitions for demo-page components. Lives in a separate
 * .ts file (not inside a .svelte component) so re-exports work cleanly.
 */
export type DemoRoute = 'mess3' | 'bio' | 'xor' | 'brackets' | 'rnn';
